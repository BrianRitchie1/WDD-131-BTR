document.querySelector('button').addEventListener('click', () =>{
    
    const grandes = document.querySelector('input').value;

    const gradeArray = grades.split(',');

    let gpa = 0;
    let totalCreditz = 0;
    gradeArray.forEach(element => {
        
    });
    
    console.log("deez")
});
    
